spring:
  docker.compose:
    enabled: true

  jpa:
    show-sql: true
    properties.hibernate:
        format_sql: true

  logging:
    level:
      org.springframework.jdbc.datasource.init: DEBUG
      org.springframework.ai: DEBUG